{{- if .Values.secret.create -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  labels:
    {{- include "kbot.labels" . | nindent 4 }}
type: Opaque
data:
  tele-token: {{ .Values.secret.tele-token | b64enc }}
{{- end }}

